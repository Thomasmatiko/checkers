name: drafts_game

description: An advanced Drafts and Checkers game built with Flutter.

publish_to: 'none'

version: 1.0.0+1

environment:
 sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: 1.0.8
  shared_preferences: 2.5.3
  audioplayers: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/sounds/
